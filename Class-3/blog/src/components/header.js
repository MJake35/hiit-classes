import '../styles/header.css'

const Header = () => {
    return (
        <header>
            <h1>Fakebook</h1>
            <button>Logout</button>
        </header>
    );
}

export default Header;